import { MinutosAHoraPipe } from './minutos-ahora.pipe';

describe('MinutosAHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutosAHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
