export class Country {
    // Define attributes
    public name?: string;
    public flag?: string;
  
    constructor(name?: string, flag?: string) {
      // Initialize attributes in the constructor
      this.name = name;
      this.flag = flag;
    }
  }